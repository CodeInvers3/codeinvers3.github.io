backend:
  name: git-gateway
  branch: master
  media_folder: "images"
  media_library: "files"
  collections:
  - name: "posts"
    label: "Post"
    folder: "_posts"
    filter:
      field: language
      value: en
    fields:
      - { label: "Language", name: "language" }
